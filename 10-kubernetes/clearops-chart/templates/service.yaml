apiVersion: v1
kind: Service
metadata:
  name: {{ include "clearops-chart.fullname" . }}
  labels:
    {{- include "clearops-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clearops-chart.selectorLabels" . | nindent 4 }}
