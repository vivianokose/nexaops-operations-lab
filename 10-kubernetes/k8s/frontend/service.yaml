apiVersion: v1
kind: Service
metadata:
  name: clearops-frontend
  namespace: clearops
spec:
  type: NodePort
  selector:
    app: clearops-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
